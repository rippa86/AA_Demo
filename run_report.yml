- name: create repo
  hosts: aap
  tasks:
    - ansible.builtin.include_tasks: tasks/git_config.yml

- name: Run Scap Report on Host
  hosts: rhelz
  tasks:
    - name: ping host
      ansible.builtin.ping:
    
    - name: run the openscap report
      ansible.builtin.include_tasks: tasks/execite_openscap.yml
      vars:
        re_run: false